# React Native iOS project

- This repository uses the React Native Community CLI and a versioned native Xcode project. Do not add Expo runtime packages or run Expo prebuild.
- Keep native changes in `ios/` and open `ios/BanjiaTiaoli.xcworkspace` when testing with Xcode.
- Check the React Native 0.81 documentation before changing native integration or build configuration.
